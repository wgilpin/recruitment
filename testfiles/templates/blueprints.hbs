<div>
  {{#each result}
  <div class="fancy">
    <button class="blueprint">
      {{type_id}}
    </button>
    <div class="allinside">
      <!-- TODO: process -->
      {{process result}}
    </div>
  </div>
  {{/each}}
</div>